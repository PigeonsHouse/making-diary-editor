# 製作日誌動画エディター 設計

## 1. 目的と前提

本アプリは、日々の製作記録を会話形式の動画へ編集・書き出しする、単一ユーザー向けWebアプリである。

- PCまたは自宅サーバー上でDocker Composeにより稼働する
- LAN内のスマートフォンや別PCからIPアドレスでアクセスできる
- インターネットへの直接公開は対象外とする
- 認証、ユーザー管理、共同編集は初期版の対象外とする
- 動画プロジェクトの日数や期間は制限しない。3日、1か月、歯抜け日付を許容する
- キャラクター設定は全プロジェクト共通とし、変更は過去プロジェクトにも即時反映する
- 過去状態との互換性や再現性は保証しない。不整合は検証エラーとして表示する

## 2. 採用技術

| 領域 | 採用技術 |
| --- | --- |
| フロントエンド・API | Next.js / React / TypeScript |
| フォーム | React Hook Form |
| スキーマ検証 | Zod |
| DB | PostgreSQL |
| ORM | Drizzle ORM |
| 動画プレビュー | `@remotion/player` |
| 動画レンダリング | Remotion |
| レンダージョブ | Redis / BullMQ |
| ジョブ進捗通知 | SSE |
| PSD解析・合成 | `ag-psd` |
| 素材変換 | FFmpeg / FFprobe |
| 音声合成 | VOICEVOX Engine |
| 会話生成 | Gemini API |
| パッケージ管理 | pnpm |
| 単体・結合テスト | Vitest |
| E2Eテスト | Playwright |

Remotion Studioは使用しない。編集画面は独自UIとし、プレビューの再生エンジンとしてのみ`@remotion/player`を使用する。

## 3. Docker構成

Docker Composeで次の5サービスを起動する。

| サービス | 責務 |
| --- | --- |
| app | Next.js UI、API、素材受付、Gemini連携、音声生成指示 |
| renderer | BullMQワーカー、Remotionレンダリング、素材変換 |
| voicevox | VOICEVOX Engine |
| postgres | 永続データ |
| redis | レンダー・素材変換ジョブ |

アップロード素材、生成音声、PSD合成PNG、レンダリング結果はDockerの永続ボリュームへ保存する。DBとファイルボリュームは一緒にバックアップする必要がある。

## 4. 動画の論理構造

```text
プロジェクト
├─ プロジェクト名
├─ 登場キャラクター順
├─ 立ち絵配置設定
├─ 今作りたいものセクション（任意、最大1件）
│  ├─ Markdownリスト
│  └─ セリフ列
└─ 日誌（0件以上、手動並び順）
   ├─ 日付
   ├─ サブタイトル
   └─ コンテンツブロック（0件以上）
      ├─ 背景素材（動画、画像、未指定のいずれか）
      ├─ セリフ列
      └─ ブロック末尾余白（任意）
```

- プロジェクト名はエディター上の識別にのみ使い、動画内には表示しない
- 日誌の日付は重複不可とする
- 日誌は任意順で保持し、「日付順に並べる」操作を提供する
- 日付順への自動並べ替えは常時強制しない
- コンテンツブロックは並べ替え可能とする
- セリフは同一ブロック内および別ブロックへ移動可能とする
- 任意のセリフ位置から新しいブロックへ分割できる
- 無言ブロックを許可する

## 5. 時間計算

### 5.1 基本方針

- 動画上の絶対秒、開始フレーム、終了フレームをユーザーに入力させない
- VOICEVOXが生成した音声尺からすべての位置を自動計算する
- 音声または設定が変化した場合、それ以降の時刻を再計算する
- フレームへの変換時にのみ30fpsへ丸める

### 5.2 セリフ前余白

キャラクターは`defaultPauseBeforeSeconds`を持ち、セリフは任意の`pauseBeforeSeconds`で上書きできる。正数、0、負数を許容する。

ブロック内の2件目以降について、セリフ`i`の時刻を次で求める。

```text
pause(i) =
  セリフに明示値があればその値
  なければ話者の defaultPauseBeforeSeconds

start(i) = audioEnd(i - 1) + pause(i)
audioEnd(i) = start(i) + audioDuration(i)
```

- 正数は、前の音声終了後も前の字幕を表示する時間である
- 0は前の音声終了直後に発話する
- 負数は前の発話へ被せる
- `start(i) < start(i - 1)`となる指定は不正値とする
- 同時開始となる`start(i) === start(i - 1)`は許容する

ブロック先頭のセリフは次の例外を持つ。

- `pauseBeforeSeconds`未指定なら、キャラクター既定値を使わず0秒とする
- 明示指定があれば適用する
- 明示値によってブロック開始前になる負数は不正値とする

### 5.3 字幕の被り連鎖

隣接するセリフについて、後の発話が前の音声終了前に始まる場合、それらを同じ被りグループとする。被りが連鎖する限り同じグループへ含める。

- グループ内の全字幕を、グループ最後の字幕表示終了まで残す
- 3件以上の被りを許容する
- 字幕は積み上げず、通常時と同じ中央位置へ重ねる
- 新しい字幕ほど手前へ描画する
- 古い字幕が読みにくくなることは意図した演出とする

被りがない場合、字幕は次のセリフ開始まで表示する。ブロック末尾では末尾余白が終わるまで表示する。

### 5.4 ブロック末尾余白

ブロック末尾余白は0以上とする。次の優先順で決定する。

1. ブロックの`endHoldSeconds`
2. アプリ全体設定の`defaultBlockEndHoldSeconds`

アプリ全体設定の初期値は0.5秒とし、設定画面から変更可能にする。ブロック切り替えはクロスフェードせずカットとする。

### 5.5 無言ブロック

- セリフが0件の場合は`durationSeconds`を必須とする
- セリフが1件以上の場合は`durationSeconds`を指定不可とする
- `durationSeconds`とセリフ由来の尺を併用しない

## 6. 日誌イントロ

各日誌の先頭に、コンテンツブロックとは別の固定尺イントロを追加する。

```ts
const DATE_CENTER_DURATION_SECONDS = 0.8;
const DIARY_UI_FADE_DURATION_SECONDS = 0.3;
```

1. 最初の0.8秒は日付を画面中央へ表示する
2. 日付中央表示をカットアウトする
3. 左上の日付、右上のサブタイトル、字幕背景矩形を0.3秒でフェードインする
4. フェード完了後、最初のセリフを開始する

合計1.1秒は日誌の尺へ追加される。イントロ中も次を表示・再生する。

- 最初のコンテンツブロックの背景素材
- 最初のコンテンツブロックの素材音声
- 全登場キャラクターの立ち絵

最初のブロックに動画がある場合、会話開始時点で動画は1.1秒進んでいる。尺合わせ再生の対象尺にも、この1.1秒を含める。

日誌にコンテンツブロックがない場合は、基本背景と既定表情の立ち絵でイントロのみを表示する。

## 7. 背景素材

### 7.1 取り込み

- 素材はWeb UIからのアップロードだけを扱う
- サーバー上の任意パス参照は提供しない
- 原本は削除せず永続保存する
- 初期版では素材削除UIを提供しない
- 未使用素材も保持する

動画はアップロード後にFFprobeで解析し、プレビュー・レンダリング用のH.264/AAC MP4へ変換する。回転メタデータは画素へ反映する。

画像はJPEG、PNG、WebPを受け付け、向きを正規化する。PSDはキャラクター素材として別に扱う。

変換中または変換失敗した素材を使うブロックはレンダリング不可とする。アップロード容量のアプリ独自上限は設けない。

### 7.2 表示

- 1920×1080の画面縁から上下左右32pxの内側へ表示する
- トリム後の素材全体を`contain`で中央配置する
- 位置移動、任意拡大、回転、キーフレームは初期版では提供しない
- 上、右、下、左について、元素材ピクセル単位のトリム値を指定できる
- トリム結果はエディターでプレビューする
- 素材未指定時は、白背景にグレーのグリッドを表示する

### 7.3 動画設定

動画素材は次を設定できる。

- 素材内の開始位置
- 素材内の終了位置
- 音量（既定で音声あり）
- ブロック尺に不足する場合の挙動

尺不足時の挙動は次から選ぶ。

| 種別 | 挙動 |
| --- | --- |
| loop | 指定区間をループ再生する |
| freeze | 最終フレームを静止表示する |
| fade-out | 最終フレームから基本背景へフェードアウトする |
| fit-duration | ブロック全尺へ合うよう再生速度を自動調整する |

`fade-out`の時間は任意指定とし、未指定時は0.5秒とする。ブロック残り時間より長い場合は、ブロック終了時に途中終了する。

`fit-duration`は速度倍率を制限しない。この方式を選んだ動画の素材音声は強制的に音量0とし、音量入力を無効化する。

## 8. 字幕と立ち絵

### 8.1 日誌字幕

- 下半分に、半透明の黒へわずかに紺を加えた字幕背景矩形を表示する
- 字幕は矩形内で中央寄せする
- 話者名は表示しない
- 文字色は白、縁取りは話者のイメージカラーとする
- 各セリフ単体で領域へ収まるか検証する
- 収まらない場合は最小値までフォントを縮小する
- 最小フォントでも収まらなければレンダリング不可とする

### 8.2 立ち絵配置

プロジェクトは、グローバルなキャラクターから「登場キャラクター順」を持つ。順序はエディターで変更できる。

- 0番目を右、1番目を左、以降は右、左と交互配置する
- 日誌イントロから日誌終了まで全員を常時表示する
- プロジェクトの`basePeekOffsetPx`で、左右1人目を字幕矩形上辺から何px見せるか指定する
- プロジェクトの`stackStepPx`で、同じ側の2人目以降を直下のキャラクターから何px上へずらすか指定する
- 同じ側では、順序が後のキャラクターほど奥へ重ねる

初期状態はキャラクター既定のPSDレイヤー選択とする。セリフ開始時に話者をセリフ指定の表情へ切り替え、その日誌内では次に同じ話者が話すまで維持する。日誌が変わると既定表情へ戻す。

## 9. 今作りたいものセクション

- プロジェクトごとに任意で1件持つ
- 動画の冒頭、全日誌より前へ固定配置する
- 罫線ノート風の専用背景を使う
- Markdownテキストをエディターで直接編集する
- 動画側では黒に近い文字色の、中黒付き・インデント対応リストとして表示する
- 初期版で保証するMarkdownはネストした順不同リストとプレーンテキストに絞る
- HTMLは無効化・サニタイズする
- 立ち絵と字幕背景矩形は表示しない
- セリフは画面下部中央へ表示し、縁取りとドロップシャドウで可読性を確保する
- セリフの時間計算、被り、末尾余白、収まり検証は日誌と共通とする
- セリフがない場合は表示秒数を必須とする

## 10. キャラクター

キャラクターはプロジェクトから独立したグローバルデータとする。

```text
キャラクター
├─ 表示名
├─ VOICEVOXキャラクター名
├─ イメージカラー
├─ 性格・口調メモ
├─ PSD原本
├─ PSD表示設定
│  ├─ 選択グループ
│  ├─ グループごとの候補
│  └─ 既定レイヤー選択
├─ 立ち絵調整
│  ├─ scale
│  ├─ offsetX
│  └─ offsetY
└─ セリフ既定値
   ├─ VOICEVOXスタイル
   ├─ 話速
   ├─ ピッチ
   ├─ 抑揚
   ├─ 音量
   ├─ セリフ前余白
   └─ PSDレイヤー選択
```

セリフはVOICEVOX設定とPSD選択を任意で上書きできる。未指定値は、レンダリング時点の最新キャラクター設定から解決する。

キャラクター削除やPSDレイヤー変更によって参照が不正になった場合、自動修復しない。該当箇所を示し、修正されるまでレンダリングを禁止する。

## 11. PSD処理

初期版は`dialy-video`の方式を踏襲する。

- `ag-psd`でPSDTool互換のレイヤーツリーを読み込む
- キャラクター設定で選択グループと候補を作る
- キャラクター既定値とセリフ上書きをマージする
- レイヤー組み合わせから透過PNGを事前合成する
- PSD内容と選択値のハッシュをキーにキャッシュする
- 同一組み合わせは再生成しない
- 発話中の口パク、瞬きは初期版では行わない

元PSDとレイヤー選択情報を保持し、将来の口パク・瞬きへ拡張できるようにする。ただし、未確定のアニメーション設定は初期データモデルへ追加しない。

## 12. VOICEVOX

### 12.1 自動生成

次の値が変わったとき、debounce後に音声を自動生成する。

- 表示セリフ
- AquesTalk風kana
- キャラクター
- VOICEVOXスタイル
- 話速
- ピッチ
- 抑揚
- 音量

入力値とVOICEVOX設定のハッシュで生成音声をキャッシュする。

- 生成中はセリフに進捗表示を出す
- 入力が再変更された場合、古い生成結果を採用しない
- 生成完了後にタイムラインとプレビューを再計算する
- VOICEVOX停止中もテキスト編集は続行できる
- 未生成または生成失敗の音声があればレンダリング不可とする
- 「全音声を再生成」操作を提供する

## 13. Gemini会話生成

生成単位は日誌1日分とする。

### 入力

- ユーザーが入力した、その日にやったことのメモ
- プロジェクトの登場キャラクター
- 各キャラクターの表示名、性格、口調メモ

### 出力

順序付きの次の値だけを構造化出力させる。

```ts
type GeneratedDialogue = {
  characterId: string;
  text: string;
};
```

コンテンツ分割、素材、kana、VOICEVOXパラメータ、PSD表情、余白は推論させない。

### 反映

1. 生成結果を確認する
2. 「追加」を押す
3. 選択中の日誌末尾に素材未設定のコンテンツブロックを1件作る
4. 全生成セリフをそのブロックへ追加する
5. ユーザーが分割、移動、編集、削除する

既存セリフの置換は行わない。Gemini APIキーとモデル名はサーバー環境変数で管理し、ブラウザへ渡さない。

## 14. 編集UI

### 14.1 主要画面

- プロジェクト一覧
- プロジェクト編集
- キャラクター設定
- アプリ全体設定
- 素材一覧
- レンダリング履歴

### 14.2 プロジェクト編集

デスクトップでは、セリフ中心の縦スクロール編集領域と、追従する動画プレビューを並べる。

- 日誌、ブロック、セリフをカードで階層表示する
- セリフ選択時にプレビューをそのセリフ開始位置へシークする
- セリフ、ブロック、日誌を並べ替えできる
- セリフ移動にはドラッグ操作と上下移動操作の両方を用意する
- 詳細度の低い項目を初期表示し、音声・PSD・余白上書きは展開式にする
- 入力とプレビューを同じ画面で完結させる

### 14.3 スマートフォン

スマートフォンでは次を提供する。

- プロジェクト閲覧
- セリフの追加、編集、削除、並べ替え
- セリフのブロック間移動
- 音声確認
- 動画プレビュー

PSDレイヤー設定、細かなトリム編集、レンダリング設定は閲覧のみでもよい。タブレットとPCでは全機能を提供する。

## 15. 自動保存と競合

- 入力はクライアント側で即時反映し、debounceしてAPIへ保存する
- プロジェクト全体ではなく、日誌、ブロック、セリフなどの単位で更新する
- 更新対象はリビジョン番号を持つ
- 古いリビジョンによる更新はHTTP 409で拒否する
- 競合時は「別端末で更新されています。再読み込みしてください」と表示する
- 別端末の変更をリアルタイム同期しない
- Undo/Redoはブラウザを閉じるまでのクライアント履歴とする
- 永続的な操作履歴、版管理、削除復元は初期版では提供しない

## 16. レンダリング

### 16.1 出力

- 1920×1080
- 30fps
- MP4
- H.264映像
- AAC音声

解像度、FPS、コーデックは初期版ではGUI変更不可とし、コード定数として分離する。縦動画、4K、60fpsは対象外とする。

### 16.2 ジョブ

- アプリは検証済みデータのスナップショットを作り、BullMQへ投入する
- レンダラーは同時に1ジョブだけ処理する
- 追加要求はRedis上で待機する
- 進捗をSSEで編集画面へ通知する
- レンダリング中も編集を継続できる
- ジョブ開始後の編集は実行中ジョブへ反映しない
- 完成ファイルをサーバーへ保持し、ブラウザからダウンロードできる
- 再レンダリング時も過去出力を削除しない

## 17. 主なデータ

詳細なDB物理設計は実装時に分割するが、少なくとも次のエンティティを持つ。

| エンティティ | 役割 |
| --- | --- |
| app_settings | エディター共通値 |
| characters | グローバルキャラクター |
| character_psd_configs | PSDツリーと選択グループ |
| projects | プロジェクト名と立ち絵配置 |
| project_characters | 登場キャラクター順 |
| wish_lists | 今作りたいものMarkdown |
| diary_entries | 日付、サブタイトル、並び順 |
| content_blocks | 素材、無言尺、末尾余白、並び順 |
| dialogues | 話者、本文、kana、余白、並び順 |
| dialogue_voice_overrides | セリフ単位VOICEVOX上書き |
| dialogue_psd_overrides | セリフ単位PSD上書き |
| assets | 原本、変換物、メタデータ、処理状態 |
| generated_audio | 入力ハッシュ、音声、尺、生成状態 |
| psd_renders | 選択ハッシュ、透過PNG |
| render_jobs | スナップショット、状態、進捗 |
| render_outputs | 完成動画履歴 |

並び替え対象は整数の`sortOrder`を持つ。IDは推測困難なUUIDとする。削除可能な編集データには外部キー制約を設け、素材・生成物は参照状態を確認して管理する。

## 18. レンダリング前検証

次のいずれかがあればレンダリングを開始しない。

- 参照キャラクターが存在しない
- キャラクターのVOICEVOX名またはスタイルを解決できない
- PSD原本、選択グループ、レイヤー参照が不正
- 音声が未生成、生成中、生成失敗
- 素材が変換中、変換失敗、または見つからない
- トリム後の幅または高さが0以下
- 動画の開始位置と終了位置が不正
- セリフ開始が直前セリフ開始より前になる
- 先頭セリフの明示余白によって開始が0秒未満になる
- 無言ブロックの尺が未指定または0以下
- セリフありブロックに無言尺が指定されている
- ブロック末尾余白が負数
- 字幕が最小フォントでも表示領域へ収まらない
- 日誌の日付が重複している

エラーはプロジェクト全体の一覧だけでなく、修正対象のカードにも表示する。

## 19. 初期版の対象外

- 認証、複数ユーザー、権限管理
- インターネットへの直接公開
- 共同編集と別端末へのリアルタイム同期
- 素材削除と自動クリーンアップ
- 過去キャラクター設定の固定、互換性維持
- 永続Undo、変更履歴、版復元
- Remotion Studio
- 絶対時刻またはフレーム単位の手動タイムライン
- PSD立ち絵の口パク、瞬き
- 背景素材のキーフレーム、回転、パン
- コンテンツ間のクロスフェード
- 背景音声の自動ダッキング
- Geminiによる素材割り当て、コンテンツ分割、音声・表情設定
- スマートフォンでの全設定編集

## 20. 実装順

1. Docker、DB、共通スキーマ、アプリ骨格
2. キャラクターCRUD、VOICEVOX接続、PSD解析
3. プロジェクト・日誌・ブロック・セリフCRUD
4. 自動保存、並べ替え、競合検出
5. 音声自動生成と時間計算
6. RemotionコンポジションとWebプレビュー
7. 素材アップロード、変換、トリム、尺不足処理
8. Redisレンダーキュー、進捗、出力履歴
9. Gemini会話生成
10. レスポンシブ対応、検証表示、E2Eテスト

